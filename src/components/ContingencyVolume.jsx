import { useContext } from "react"
import { MapContext } from "../context/map"

export function ContingencyVolume () {

    const {markers} = useContext(MapContext)

    return (
        <></>
    )

}